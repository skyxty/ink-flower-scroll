# 花信水墨长卷 v14 改进说明

## 已完成

1. 立体视差与花朵动态
   - 增加鼠标跟随光场、前后景深与花朵呼吸漂浮。
   - 保留原有展开、墨色渐生、落花粒子和水墨晕染。
   - 支持 `prefers-reduced-motion`，减少动态偏好的用户不会看到持续漂浮。

2. 运行问题
   - 补齐 Cloudflare Worker 的本地类型声明，全项目 TypeScript 检查通过。
   - 增加缺失的预览元数据。
   - 将与当前项目无关的模板测试替换为花木素材、立体动效、键盘操作和手机端测试。

3. 页面内容与样式
   - 交互提示改为“轻触花木 · 墨色渐生”。
   - 新增十四幅花木快速导航和当前作品状态。
   - 保持原有宣纸、朱砂、诗笺和中英双语设计语言。

4. 手机端与性能
   - 为 420px 以下屏幕重新调整标题、题诗、字号、间距和进度条。
   - 首两幅图片优先加载，其余图片延迟解码和加载。
   - 粒子数由无清理的 220 个改为桌面最多 54 个、手机最多 28 个，并在动画结束后删除。
   - 滚动状态改为每帧最多更新一次，页面隐藏时暂停生成粒子。

5. 可访问性与交互
   - 支持 Enter/空格触发花开，方向键和 PageUp/PageDown 切换作品。
   - 增加可聚焦状态、导航标签和当前步骤语义。

## 验证结果

- TypeScript：通过
- Vinext production build：通过
- 自动化测试：5/5 通过
- 桌面视觉检查：1440 × 900 通过
- 手机视觉检查：390 × 844 通过

## 本地运行

依赖安装完成后，使用项目 `package.json` 中的脚本运行。Windows 环境若没有 Bash，可直接执行：

```powershell
.\node_modules\.bin\vite.CMD --host 127.0.0.1 --port 4173
```

## 下一阶段建议

- 若需要更强的真实立体感，应把每幅画的花、枝、叶和背景拆成独立透明图层；当前版本是在不重绘十四幅素材的前提下实现的 2.5D 深度。
- 可继续增加环境音、自动导览、作品目录抽屉和中英文切换，但建议作为独立功能迭代，以免削弱当前长卷的安静观赏体验。
