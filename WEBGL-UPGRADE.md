# WebGL 深度分层说明

## 渲染结构

每幅水墨图仍使用原始 PNG。着色器根据像素实时计算：

- 墨色深度：`1 - luminance`
- 花色权重：RGB 最大值与最小值之差
- 综合深度：墨色权重 + 花色权重

深度图驱动细分平面的 Z 轴位移，并拆成中景墨枝与近景彩花两层。鼠标移动时，两个平面产生不同的相机视差；花开状态进一步增加层间距离。

## 性能策略

- Three.js 组件按需加载。
- 页面只保留当前作品和相邻作品的 WebGL 场景，避免同时创建十四个上下文。
- 桌面网格为 118 × 178，手机降为 58 × 88。
- 像素比限制在 1～1.45。
- 低动态偏好下停止逐帧渲染。
- WebGL 不可用或尚未加载时，原始 CSS 水墨图仍然可见。

## 主要文件

- `app/components/FlowerDepthScene.tsx`：WebGL 场景、深度着色器和自适应网格。
- `app/page.tsx`：按当前卷页挂载场景。
- `app/globals.css`：Canvas 层级、透明度和回退样式。

## 开发工具

- Three.js
- React Three Fiber
- Vinext / React
- 浏览器 WebGL 2

若未来能获得人工拆分的透明花、枝、叶图层，可以直接替换当前实时阈值遮罩，获得更精确的遮挡边缘和更大的镜头运动范围。
